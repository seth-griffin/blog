
Quick Start
=====================
flask run --port 5001


Next:
====================
-- Add content database (maria nosql, dml management tool, ddl, orm)
    -- continue with sqlalchemy
        -Create db model
        -DB creation
        -DB Migration
        -Intial db load 
